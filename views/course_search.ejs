<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <script type="text/javascript" src="/javascripts/jquery-2.1.4.min.js"></script>
  <script type="text/javascript" src="/javascripts/bootstrap.min.js"></script>
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <link rel='stylesheet' href='/stylesheets/bootstrap.min.css' />
  <link rel='stylesheet' href='/stylesheets/bootstrap-theme.min.css' />
</head>
<body>
  <span>
  <h1>Course Search Results</h1>
  <% if (result == null || result.courses == null || result.courses.length == 0) { %>
    <p>No matches</p>
  <% } else { %>
    <% for(var i=0; i<result.courses.length; i++) { %>
        <p>
            <div>
                <span><%= result.courses[i].courseId %></span>
                &nbsp;
                <span><%= result.courses[i].courseTitle %></span>
            </div>
            <div><%= result.courses[i].courseDescription %></div>
        </p>
      <% } %>
    <% } %>
  </span>
</body>
</html>
