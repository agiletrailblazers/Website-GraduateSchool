<!DOCTYPE html>
<html>
  <head>
    <title> Graduate School USA | <%= title %></title>
    <% include ../../partials/head %>
    <script type="text/javascript" src="/javascripts/manage/cart/cart.js"></script>
  </head>
  <body>
    <% include ../../partials/header %>
    <header>
      <div id="container" class="container">
        <div class="loading" style="display:none;"></div>
        <div id="outer" class="col-lg-12 col-md-12 col-sm-12 form-fix padding20">

          <div id="gs-form">
              <div id="cart">
                <form id="cart-form" role="form">
                  <div id="form-wrap">
                    <div id="cart-items">
                      <table>
                        <tr>
                          <th>Course</th>
                          <th>Session</th>
                          <th>Type</th>
                          <th>Cost</th>
                        </tr>
                        <tr>
                          <td><%= course.title %></td>
                          <td>
                            <%= session["startDate"].slice(0,-6) %> - <%= session["endDate"]%> <br/>
                              <% if (session["startTime"] !== null || session["endTime"] !== null) { %>
                                <%= session["startTime"]%> - <%= session["endTime"]%>
                              <% } %>
                              <% if (session["days"] !== null) { %>
                              , <%= session["days"] %>
                              <% } %>
                          </td>
                          <td><%= course.type %></td>
                          <td>$<%= session["tuition"] %></td>
                        </tr>
                      </table>
                    </div>
                    <div class="spacer10"></div>
                    <div id="buttons" class="pull-right">
                      <button id="cart-form-continue" type="submit" tabindex="21" onclick="window.location.href='#gs-form'">Continue</button>
                    </div>
                  </div> <!-- END form-wrap -->
                </form> <!-- END cart-form -->
            </div>  <!-- END cart -->
          </div> <!--END gs-form-->

        </div> <!--END outer-->
      </div> <!--END container-->
    </header>
    <!-- Footer -->
    <% include ../../partials/footer %>
    <!-- END Footer  -->
    <!-- Chat  -->
    <% include ../../partials/chat %>
    <!-- END of Chat  -->
  </body>
</html>
