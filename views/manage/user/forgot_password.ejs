<!DOCTYPE html>
<html>
  <head>
    <title> Graduate School USA | <%= title %></title>
    <% include ../../partials/head %>
    <script type="text/javascript" src="/javascripts/manage/alerts.js"></script>
  </head>
  <body>
    <% include ../../partials/header %>
      <div id="container" class="container">

        <% if ((typeof(userNotFound) != 'undefined') && userNotFound) { %>
        <div id="gs-alert-error" class="alert alert-danger" role="alert">
        <% } else { %>
        <div id="gs-alert-error" class="alert alert-danger" role="alert" style="display:none;">
        <% } %>
          <span id="gs-alert-remove" class="glyphicon glyphicon-remove pull-right" aria-hidden="true"> </span>
          <p>You must enter a valid email address.</p>
        </div>

        <% if ((typeof(passwordReset) != 'undefined') && passwordReset) { %>
          <div id="gs-alert-success" class="alert alert-success" role="alert">
            <p>A temporary password has been sent to the email address associated with your account. Please use this temporary password to sign in to your account and create a new secure password.</p>
          </div>
        <% }
           else {
        %>

          <div class="row">
            <div id="outer" class="col-lg-12 col-md-12 col-sm-12 content-offset">
              <div id="gs-form">
                <div id="forgot-password">
                  <div class="row">
                    <div class="col-lg-12">
                      <p>Enter the email address associated with your Graduate School USA account, then click 'Submit'.  A new temporary password will be created and emailed to you.</p>
                    </div>
                    <form id="login-form" role="form" method="post" action="/manage/user/password/forgot">
                      <div class="col-lg-12">
                        <div class="form-inline">
                          <span class="form-group">
                            <label for="email" >Email</label>
                            <input type="email" id="email" name="email" class="form-control" size="50" tabindex="11">
                          </span>
                        </div>
                        <div class="spacer25"></div>
                        <div id="buttons" class="pull-left">
                          <button id="forgot-password-continue" type="submit" tabindex="12">Submit</button>
                        </div> <!--END buttons-->
                      </div>
                    </form>
                  </div>
                </div>  <!-- END fogot-password -->
              </div> <!--END gs-form-->
            </div> <!--END outer-->
          </div> <!--END row -->
        <% } %>

      </div> <!--END container-->
    <div class="spacer60 non-mobile"></div>
    <div class="spacer60 non-mobile"></div>
    <div class="spacer mobile"></div>
    <!-- Footer -->
    <% include ../../partials/footer %>
    <!-- END Footer  -->
    <!-- Chat  -->
    <% include ../../partials/chat %>
    <!-- END of Chat  -->
  </body>
</html>
