<!DOCTYPE html>
<html>
  <head>
    <title> Graduate School USA | <%= title %></title>
    <meta charset="utf-8">
      <% include partials/head %>
    <script type="text/javascript" src="/javascripts/catalogs.js"></script>
    <link rel="stylesheet" href="/stylesheets/generic_detail.css">
  </head>
  <body>
    <% include partials/header %>
    <div id="main-container" class="container">
      <div class="row">
        <a href="/under-construction">Archived catalogs</a>.
        <div class="col-lg-12">
          <div id="gs-tabs">
            <div class="tabs">
              <ul class="nav nav-tabs">
                <% entry.forEach(function(entrySection,num) { %>
                  <% if (num == 0) { %>
                    <li class="active"><a data-toggle="tab" href="#tab<%=num%>"><%=entrySection.catlogTitle %></a></li>
                  <% } else { %>
                    <li><a data-toggle="tab" href="#tab<%=num%>"><%=entrySection.catlogTitle %></a></li>
                  <% } %>
                <% }); %>
              </ul>
            </div>
            <div class="panel panel-default gs">
              <div class="panel-body">
                <div class="tab-content">
                  <% entry.forEach(function(entrySection,num) { %>
                  <% if (num == 0) { %>
                    <div id="tab<%=num%>" class="tab-pane fade in active">
                  <% } else { %>
                    <div id="tab<%=num%>" class="tab-pane fade">
                  <% } %>
                  <div class="col-lg-6 col-md-6 col-sm-12" id="col1">
                    <% entrySection.assetList.forEach(function(asset) { %>
                      <%  if (asset.displayInSecondColumn==false) {%>
                        <p class="asset" data-toggle="tooltip" title="<%= asset.description %>" data-contenttype="<%= asset.catalogFilter %>"><a href=<%= asset.url %>  <span><img src="/images/pdf.png"/></span> <%= asset.title %></a></p>
                      <% } %>
                    <% }) %>
                  </div>
                  <div class="col-lg-6 col-md-6 col-sm-12" id="col2">
                    <% entrySection.assetList.forEach(function(asset) { %>
                      <%  if (asset.displayInSecondColumn) {%>
                        <p class="asset"  data-toggle="tooltip" title="<%= asset.description %>" data-contenttype="<%= asset.catalogFilter %>"><a href=<%= asset.url %>  <span><img src="/images/pdf.png"/></span> <%= asset.title %></a></p>
                      <% } %>
                    <% }) %>
                  </div>
                </div>
              <% }) %>
            </div> <!-- End tab-content -->
          </div>
        </div>
      </div> <!-- End GS-tabs -->
    </div> <!-- End column -->
  </div> <!-- End row -->
    <!--/nutch_noindex-->
</div> <!-- End Container -->
  <!-- Footer -->
  <% include partials/footer %>
  <!-- END Footer  -->
  <!-- Chat  -->
  <% include partials/chat %>
  <!-- END of Chat  -->
</body>
</html>
